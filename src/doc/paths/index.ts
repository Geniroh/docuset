/**
 * API Paths Index
 */

/**
 * Combined paths object for all API endpoints
 *
 * To add new endpoints:
 * 1. Create a new file in src/doc/paths/ (e.g., products.ts)
 * 2. Export a paths object (e.g., productPaths)
 * 3. Import it here and spread it into allPaths
 */
export const allPaths = {} as const;
